{
  "name": "@tee-agent/contracts",
  "version": "0.1.0",
  "description": "Solidity contracts for the tee-agent (ERC-8004, ERC-7857)",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "hardhat build && node scripts/gen-abis.mjs",
    "test": "hardhat test && forge test --config-path foundry.toml",
    "test:foundry": "forge test --config-path foundry.toml",
    "clean": "hardhat clean",
    "node": "hardhat node",
    "deploy:localOracle": "yes | hardhat ignition deploy ignition/modules/DeployLocalOracle.ts --network localhost --reset --deployment-id chain-31337-local-oracle --parameters ignition/parameters.localOracle.json && npm run setup-env:localOracle",
    "deploy:baseSepolia": "npm run deploy:baseSepolia:remoteOracle",
    "deploy:baseSepolia:remoteOracle": "yes | hardhat ignition deploy ignition/modules/Deploy.ts --network baseSepolia --verify --reset --deployment-id base-sepolia-remote-oracle --parameters ignition/parameters.baseSepolia.json && BASE_SEPOLIA_ORACLE=remote npm run setup-env",
    "deploy:baseSepolia:localOracle": "yes | hardhat ignition deploy ignition/modules/DeployLocalOracle.ts --network baseSepolia --verify --reset --deployment-id base-sepolia-local-oracle --parameters ignition/parameters.baseSepolia.localOracle.json && BASE_SEPOLIA_ORACLE=local npm run setup-env",
    "deploy:base": "yes | hardhat ignition deploy ignition/modules/Deploy.ts --network base --verify --reset --parameters ignition/parameters.base.json && npm run setup-env",
    "setup-env": "node scripts/setup-env.mjs",
    "setup-env:baseSepolia:remoteOracle": "BASE_SEPOLIA_ORACLE=remote npm run setup-env",
    "setup-env:baseSepolia:localOracle": "BASE_SEPOLIA_ORACLE=local npm run setup-env",
    "setup-env:localOracle": "LOCAL_ORACLE=local npm run setup-env"
  },
  "devDependencies": {
    "@tee-agent/agent": "file:../packages/agent",
    "@nomicfoundation/hardhat-ignition": "^3.1.4",
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.4",
    "@openzeppelin/contracts": "^5.6.1",
    "@types/node": "^25.6.0",
    "dotenv": "^17.4.2",
    "ethers": "6.16.0",
    "forge-std": "github:foundry-rs/forge-std#v1.9.4",
    "hardhat": "^3.4.3",
    "typescript": "^6.0.3",
    "viem": "^2.49.3"
  }
}
