{
  "name": "@tee-agent/oracle",
  "version": "0.1.0",
  "private": true,
  "description": "TEE re-encryption oracle server for ERC-7857 AgentNFT secure transfers, deployed to Phala Cloud (Intel TDX)",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "deploy": "node ../../scripts/deploy-cvm.mjs",
    "dev:up": "docker compose -f docker-compose.dev.yml up -d",
    "dev:down": "docker compose -f docker-compose.dev.yml down",
    "dev:prediction-market": "npm run dev:up && DSTACK_SIMULATOR_ENDPOINT=http://localhost:8090 node --env-file=.env --import tsx src/examples/prediction-market.ts",
    "dev:web-fetcher": "npm run dev:up && DSTACK_SIMULATOR_ENDPOINT=http://localhost:8090 node --env-file=.env --import tsx src/examples/web-data-oracle.ts",
    "dev": "npm run dev:up && npm run dev:prediction-market",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@tee-agent/server": "*",
    "ethers": "6.13.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^25.6.0",
    "phala": "^1.1.19",
    "tsx": "^4.19.4",
    "typescript": "^6.0.3"
  }
}
