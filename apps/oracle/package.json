{
  "name": "@tee-agent/oracle",
  "version": "0.1.0",
  "private": true,
  "description": "TEE re-encryption oracle server for ERC-7857 AgentNFT secure transfers, deployed to Phala Cloud (Intel TDX)",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "deploy:prediction-market": "phala deploy -e .env -e ORACLE_ENTRY=examples/prediction-market.js --wait",
    "deploy:web-fetcher": "phala deploy -e .env -e ORACLE_ENTRY=examples/web-data-oracle.js --wait",
    "dev:up": "docker compose -f docker-compose.dev.yml up -d",
    "dev:down": "docker compose -f docker-compose.dev.yml down",
    "dev:prediction-market": "npm run dev:up && PORT=3001 DSTACK_SIMULATOR_ENDPOINT=http://localhost:8090 tsx src/examples/prediction-market.ts",
    "dev:web-fetcher": "npm run dev:up && PORT=3001 DSTACK_SIMULATOR_ENDPOINT=http://localhost:8090 tsx src/examples/web-data-oracle.ts",
    "dev": "npm run dev:up && npm run dev:prediction-market",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@0gfoundation/0g-ts-sdk": "^1.2.8",
    "@tee-agent/agent": "*",
    "@phala/dstack-sdk": "^0.1.5",
    "@types/cors": "^2.8.19",
    "cors": "^2.8.6",
    "eciesjs": "^0.4.16",
    "ethers": "6.13.1",
    "express": "^4.21.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^25.6.0",
    "phala": "^1.1.19",
    "tsx": "^4.19.4",
    "typescript": "^6.0.3"
  }
}
